from setuptools import setup

setup(
    name='noteserver',
    packages=['noteserver'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
    setup_requeres=[

    ],
    tests_requires=[

    ],
)